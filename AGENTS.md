# AdGuardian agent instructions

For every change read `.agents/skills/android-ad-guardian/SKILL.md` first

B4 is generic-first

Do not turn support back into an app allowlist

Keep the L1 Accessibility -> L2 OCR -> L3 YOLO cascade bounded and local

Specific app rules remain fast paths only

Keep the single APK at or below 50 MiB

After modifying files deliver the complete project archive
